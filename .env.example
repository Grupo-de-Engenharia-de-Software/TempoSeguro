# App
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SERVER_URL=https://tempo-seguro.app

# Socket.IO server URL (leave empty for same-origin in local dev)
NEXT_PUBLIC_SOCKET_URL=https://socket.tempo-seguro.app

# Server-side — Socket.IO server only (not used by Vercel)
AUTH_API_BASE_URL=https://api-dev-minimal-v6.vercel.app
ADMIN_EMAILS=admin@admin.com
ALLOWED_ORIGINS=https://tempo-seguro.vercel.app,https://localhost:3000
SSL_KEY_PATH=./localhost-key.pem
SSL_CERT_PATH=./localhost.pem
